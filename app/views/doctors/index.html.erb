<h1>Doctors</h1>

<ul>
<% @doctors.all.each do |doctor| %>
    <li>
    <%= link_to doctor.name, doctor %>  (<%= doctor.patient_count%> <%= "patient".pluralize(doctor.patient_count)%>)
    </li>
<% end %>
</ul>
