<h1>PATIENTS</h1>

<ul>
<% @patients.each do |patient| %>
    <li>
    <%= link_to patient.name, patient%>  (<%= patient.appointment_count%> <%= "appointment".pluralize(patient.appointment_count)%>)
    </li>
<% end %>
</ul>